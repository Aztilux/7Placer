declare const Toastify: any;
declare const RgbQuant: any;

interface Window {
        seven: {
            bots: Map<string, any>,
            pixelspeed: number,
            queue: Array<any>,
            inprogress: boolean,
            protect: boolean,
            tickspeed: number,
            order: string,
            multi: Object,
            dither: string,
            agressive_protection: boolean
        }
}
